# Вокруг света за 80 дней

Мини игра для VK через Python бота и веб приложение.

## Что внутри

- `bot.py` - VK бот на Python. Отправляет кнопку `Начать игру`.
- `web_app.py` - FastAPI сервер для мини приложения и API с вопросами.
- `static/` - интерфейс игры с картой, воздушным шаром и вопросами.
- `data/questions.json` - вопросы, варианты и правильные ответы.
- `scripts/extract_questions.py` - повторная выгрузка вопросов из Excel.
- `scripts/build_static.py` - сборка статической версии для VPS/хостинга.

## Локальный запуск

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
copy .env.example .env
uvicorn web_app:app --host 127.0.0.1 --port 8000
```

После запуска открыть:

```text
http://127.0.0.1:8000
```

## Запуск бота

В `.env` заполнить:

```text
VK_GROUP_TOKEN=токен_сообщества
VK_GROUP_ID=id_сообщества_без_минуса
MINI_APP_URL=https://адрес_мини_приложения
```

Затем:

```bash
python bot.py
```

Если будут известны `VK_APP_ID` и `VK_APP_OWNER_ID`, бот отправит кнопку типа `open_app`.
Если их не заполнить, будет обычная кнопка-ссылка на `MINI_APP_URL`.

## Статическая сборка

```bash
python scripts/build_static.py
```

Готовые файлы появятся в `build/`. Их можно загрузить на любой HTTPS хостинг как обычный статический сайт.

Для быстрой проверки правильных ответов можно открыть приложение с параметром:

```text
https://адрес_мини_приложения/?debug=1
```

## Логика игры

Игрок стартует из Красноярска. Вопрос задается по следующей стране маршрута.
Правильный ответ двигает воздушный шар вперед.
Неверный ответ двигает на один шаг назад.
После прохождения 13 стран показывается количество верных и неверных ответов.

## Маршрут

Красноярск -> Монголия -> Китай -> Мьянма -> Таиланд -> Индонезия -> Филиппины -> Перу -> Бразилия -> Камерун -> Уганда -> Сомали -> Индия -> Узбекистан -> Красноярск
